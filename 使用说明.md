# Python科学计算器 v2.0 - 使用说明

## 目录
1. [简介](#简介)
2. [安装与运行](#安装与运行)
3. [功能列表](#功能列表)
4. [操作指南](#操作指南)
5. [配置说明](#配置说明)
6. [故障排除](#故障排除)

---

## 简介

Python科学计算器 v2.0 是一款面向科研人员和工程技术人员的专业计算工具。它在保留基础四则运算功能的基础上，新增了三角函数、对数运算、幂运算、阶乘等10+种科学运算功能。

### 主要特性
- ✅ 基础四则运算：加、减、乘、除
- ✅ 三角函数：sin、cos、tan（支持角度/弧度切换）
- ✅ 对数运算：自然对数(ln)、常用对数(log)
- ✅ 幂运算与开方：x^y、√x
- ✅ 阶乘运算：n!
- ✅ 历史记录管理：保存、查看、导出
- ✅ 配置持久化：自动保存用户偏好设置
- ✅ 兼容模式：支持旧版输入习惯
- ✅ 结果格式化：自动科学计数法、自定义小数位数

---

## 安装与运行

### 系统要求
- Python 3.7 或更高版本
- 仅使用 Python 标准库，无需额外安装依赖

### 运行步骤

1. **下载代码**
   ```bash
   # 克隆或下载项目到本地
   cd calculator-v2.0
   ```

2. **运行程序**
   ```bash
   python main.py
   ```

3. **开始使用**
   程序启动后会显示欢迎界面和主菜单，根据提示选择功能即可。

---

## 功能列表

### 基础运算

| 运算 | 符号 | 示例 | 结果 |
|------|------|------|------|
| 加法 | + | 5 + 3 | 8 |
| 减法 | - | 10 - 4 | 6 |
| 乘法 | * | 6 * 7 | 42 |
| 除法 | / | 15 / 4 | 3.75 |

### 三角函数

| 函数 | 说明 | 示例(角度) | 示例(弧度) |
|------|------|------------|------------|
| sin | 正弦 | sin(30°) = 0.5 | sin(π/6) ≈ 0.5 |
| cos | 余弦 | cos(60°) = 0.5 | cos(π/3) ≈ 0.5 |
| tan | 正切 | tan(45°) = 1 | tan(π/4) ≈ 1 |

**注意**: tan(90°) 和 tan(270°) 等值无定义。

### 对数运算

| 函数 | 说明 | 定义域 | 示例 |
|------|------|--------|------|
| ln | 自然对数 (以e为底) | x > 0 | ln(2.718) ≈ 1 |
| log | 常用对数 (以10为底) | x > 0 | log(100) = 2 |

### 幂运算与开方

| 运算 | 符号 | 示例 | 结果 |
|------|------|------|------|
| 幂运算 | ^ 或 pow | 2^10 | 1024 |
| 平方根 | √ 或 sqrt | √16 | 4 |

### 阶乘

| 运算 | 符号 | 示例 | 结果 |
|------|------|------|------|
| 阶乘 | ! | 5! | 120 |

**限制**: 
- 仅支持非负整数
- 最大支持 n = 170（再大结果超出浮点数范围）

---

## 操作指南

### 主菜单

启动程序后，您将看到如下主菜单：

```
------------------------------------------------------------
【主菜单】
------------------------------------------------------------
  1. 基础运算 (+, -, *, /)
  2. 三角函数 (sin, cos, tan)
  3. 对数运算 (ln, log)
  4. 幂运算与开方 (x^y, √x)
  5. 阶乘 (n!)
  6. 查看历史记录
  7. 系统设置
  8. 兼容模式 (旧版输入方式)
  0. 退出程序
------------------------------------------------------------
  当前: 标准模式 | 角度单位: 角度 | 小数位: 4
------------------------------------------------------------
```

输入数字选择对应功能。

### 基础运算操作

1. 在主菜单选择 `1`
2. 输入第一个数字，如：`10`
3. 输入运算符，如：`/` 
4. 输入第二个数字，如：`3`
5. 查看结果

**示例会话**:
```
【基础运算】
  支持的运算符: + (加), - (减), * (乘), / (除)
  输入格式: 数字1 [运算符] 数字2
  示例: 5 + 3

请输入第一个数字: 10
请输入运算符 (+, -, *, /): /
请输入第二个数字: 3

  ✅ 结果: 10 / 3 = 3.3333
```

### 三角函数操作

1. 在主菜单选择 `2`
2. 选择函数（1=sin, 2=cos, 3=tan）
3. 输入角度值
4. 查看结果

**注意**: 可在设置中切换角度/弧度模式。

### 历史记录管理

#### 查看历史记录
1. 在主菜单选择 `6`
2. 显示最近20条记录
3. 按回车返回主菜单

#### 导出历史记录
1. 在主菜单选择 `7`（系统设置）
2. 选择 `6`（导出历史记录）
3. 输入文件名（或直接回车使用默认名称）
4. 文件将保存为 `.txt` 格式

#### 清空历史记录
1. 在主菜单选择 `7`（系统设置）
2. 选择 `5`（清空历史记录）
3. 确认清空

### 兼容模式

兼容模式支持旧版计算器的输入方式：

**基础运算格式**:
```
> 5 + 3
  = 8
> 10 * 2.5
  = 25
```

**科学运算格式**:
```
> sin 30
  = 0.5
> sqrt 16
  = 4
> log 100
  = 2
```

输入 `exit` 或 `q` 返回主菜单。

---

## 配置说明

### 进入设置菜单

在主菜单选择 `7`，进入系统设置：

```
【系统设置】
  1. 切换角度/弧度模式
  2. 设置小数位数
  3. 切换兼容模式
  4. 切换科学计数法
  5. 清空历史记录
  6. 导出历史记录
  7. 查看统计信息
  8. 重置所有设置
  0. 返回主菜单
```

### 配置项说明

#### 1. 角度/弧度模式
- **角度模式**: 三角函数输入使用度数（如 30°）
- **弧度模式**: 三角函数输入使用弧度（如 π/6 ≈ 0.5236）

#### 2. 小数位数
- 范围：0-10
- 默认：4
- 影响所有浮点数结果的显示精度

#### 3. 兼容模式
- **开启**: 启动后直接进入兼容模式界面
- **关闭**: 启动后显示主菜单（默认）

#### 4. 科学计数法
- **开启**: 超大数或超小数自动转为科学计数法显示
- **关闭**: 始终使用普通小数表示

#### 5. 重置所有设置
将所有配置恢复为默认值。

---

## 故障排除

### 常见问题

#### Q1: 输入数字后提示"不是有效的数字"
**原因**: 输入格式不正确  
**解决**: 
- 确保输入的是纯数字
- 支持格式：`123`, `-45.67`, `1.23e-4`
- 不支持：`1,000`（使用 `1000`）

#### Q2: 除法运算提示"除数不能为零"
**原因**: 除数输入为 0  
**解决**: 检查输入，确保除数不为零

#### Q3: 对数运算提示"输入必须大于0"
**原因**: 对数函数的定义域为 x > 0  
**解决**: 输入正数进行对数运算

#### Q4: 阶乘运算提示"负数没有阶乘"
**原因**: 阶乘仅定义于非负整数  
**解决**: 输入 0 或正整数

#### Q5: 正切函数提示"无定义"
**原因**: tan(90°)、tan(270°) 等值无定义  
**解决**: 避免输入这些特殊角度

#### Q6: 配置文件损坏
**现象**: 程序启动异常或配置不生效  
**解决**: 
1. 删除 `calculator_config.json` 文件
2. 重新启动程序，将使用默认配置

#### Q7: 历史记录文件损坏
**现象**: 历史记录无法加载  
**解决**:
1. 删除 `calculator_history.json` 文件
2. 重新启动程序，历史记录将重新开始记录

### 错误代码说明

| 错误信息 | 含义 | 解决方法 |
|----------|------|----------|
| DivisionByZeroError | 除数为零 | 检查除数输入 |
| NegativeNumberError | 负数错误 | 检查输入值是否为正数 |
| InvalidInputError | 无效输入 | 检查输入格式和范围 |
| ValidationError | 校验失败 | 按提示修正输入 |

### 联系支持

如遇到无法解决的问题，请：
1. 查看调试日志 `debug_logs.md`
2. 检查 Python 版本是否 >= 3.7
3. 确保所有项目文件完整

---

## 附录

### 键盘快捷键

在兼容模式下：
- `exit`, `q`, `quit` - 退出兼容模式
- `Ctrl+C` - 中断当前操作

### 数据文件说明

| 文件名 | 用途 | 可删除 |
|--------|------|--------|
| `calculator_config.json` | 用户配置 | ✅ |
| `calculator_history.json` | 历史记录 | ✅ |
| `calculator_history_*.txt` | 导出的历史记录 | ✅ |

### 版本历史

- **v2.0** (当前版本)
  - 模块化重构
  - 新增10+科学运算功能
  - 新增历史记录和配置管理
  - 新增兼容模式
