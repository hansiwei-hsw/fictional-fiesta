# API对接文档

## 一、概述

本文档详细说明智能翻译小程序支持的各翻译引擎API对接方式，包括百度翻译、有道翻译和谷歌翻译。

---

## 二、百度翻译API

### 2.1 申请流程

1. 访问 [百度翻译开放平台](https://fanyi-api.baidu.com/)
2. 注册账号并登录
3. 进入「管理控制台」→「开发者信息」
4. 创建应用，获取 `APP ID` 和 `密钥`

### 2.2 API接口信息

| 项目 | 说明 |
|------|------|
| 接口地址 | `https://fanyi-api.baidu.com/api/trans/vip/translate` |
| 请求方式 | GET |
| 字符编码 | UTF-8 |

### 2.3 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| q | string | 是 | 待翻译文本 |
| from | string | 是 | 源语言（auto表示自动检测） |
| to | string | 是 | 目标语言 |
| appid | string | 是 | APP ID |
| salt | string | 是 | 随机数 |
| sign | string | 是 | 签名 |

### 2.4 签名生成规则

```
sign = MD5(appid + q + salt + 密钥)
```

### 2.5 响应格式

```json
{
    "from": "en",
    "to": "zh",
    "trans_result": [
        {
            "src": "hello",
            "dst": "你好"
        }
    ]
}
```

### 2.6 错误码

| 错误码 | 说明 |
|--------|------|
| 52000 | 成功 |
| 52001 | 请求超时 |
| 52002 | 系统错误 |
| 52003 | 未授权用户 |
| 54000 | 必填参数为空 |
| 54001 | 签名错误 |
| 54003 | 访问频率受限 |
| 58000 | 客户端IP非法 |
| 58001 | 语言不支持 |

### 2.7 配置示例

在程序中选择「系统设置」→「配置API密钥」→「百度翻译」，输入：
- APP ID: `你的APP ID`
- Secret Key: `你的密钥`

---

## 三、有道翻译API

### 3.1 申请流程

1. 访问 [有道智云](https://ai.youdao.com/)
2. 注册账号并登录
3. 进入「控制台」→「应用管理」
4. 创建应用，选择「文本翻译」服务
5. 获取 `应用ID` 和 `应用密钥`

### 3.2 API接口信息

| 项目 | 说明 |
|------|------|
| 接口地址 | `https://openapi.youdao.com/api` |
| 请求方式 | GET |
| 字符编码 | UTF-8 |

### 3.3 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| q | string | 是 | 待翻译文本 |
| from | string | 是 | 源语言 |
| to | string | 是 | 目标语言 |
| appKey | string | 是 | 应用ID |
| salt | string | 是 | 随机数 |
| sign | string | 是 | 签名 |
| signType | string | 是 | 签名类型（v3） |
| curtime | string | 是 | 当前时间戳 |

### 3.4 签名生成规则

```
input = q[0:10] + len(q) + q[-10:]  (当q长度>20时)
input = q  (当q长度<=20时)
sign = SHA256(appKey + input + salt + curtime + 密钥)
```

### 3.5 响应格式

```json
{
    "errorCode": "0",
    "query": "hello",
    "translation": ["你好"],
    "basic": {
        "phonetic": "həˈləʊ",
        "uk-phonetic": "həˈləʊ",
        "us-phonetic": "hɛˈloʊ",
        "explains": ["int. 喂；哈罗"]
    },
    "web": [
        {
            "key": "Hello",
            "value": ["你好", "您好", "哈啰"]
        }
    ]
}
```

### 3.6 错误码

| 错误码 | 说明 |
|--------|------|
| 0 | 成功 |
| 20 | 要翻译的文本过长 |
| 30 | 无法进行有效翻译 |
| 40 | 不支持的语言类型 |
| 50 | 无效的key |
| 60 | 无词典结果 |
| 110 | 无相关服务 |
| 202 | 签名检验失败 |

### 3.7 配置示例

在程序中选择「系统设置」→「配置API密钥」→「有道翻译」，输入：
- App Key: `你的应用ID`
- App Secret: `你的应用密钥`

---

## 四、谷歌翻译API

### 4.1 申请流程

1. 访问 [Google Cloud Console](https://console.cloud.google.com/)
2. 创建项目并启用 Cloud Translation API
3. 创建服务账号或API密钥
4. 获取 `API Key`

### 4.2 API接口信息

| 项目 | 说明 |
|------|------|
| 接口地址 | `https://translation.googleapis.com/language/translate/v2` |
| 请求方式 | POST |
| 字符编码 | UTF-8 |

### 4.3 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| q | string | 是 | 待翻译文本 |
| source | string | 否 | 源语言 |
| target | string | 是 | 目标语言 |
| format | string | 否 | 文本格式（text/html） |
| key | string | 是 | API密钥（URL参数） |

### 4.4 请求示例

```
POST https://translation.googleapis.com/language/translate/v2?key=API_KEY

{
    "q": "hello",
    "source": "en",
    "target": "zh-CN",
    "format": "text"
}
```

### 4.5 响应格式

```json
{
    "data": {
        "translations": [
            {
                "translatedText": "你好",
                "detectedSourceLanguage": "en"
            }
        ]
    }
}
```

### 4.6 错误响应

```json
{
    "error": {
        "code": 400,
        "message": "Invalid Value",
        "status": "INVALID_ARGUMENT"
    }
}
```

### 4.7 常见错误

| 错误 | 说明 |
|------|------|
| 400 | 请求参数错误 |
| 401 | API密钥无效 |
| 403 | 权限不足或配额超限 |
| 429 | 请求过于频繁 |
| 500 | 服务器内部错误 |

### 4.8 配置示例

在程序中选择「系统设置」→「配置API密钥」→「谷歌翻译」，输入：
- API Key: `你的API密钥`

---

## 五、语言代码对照表

| 语言 | 百度代码 | 有道代码 | 谷歌代码 |
|------|----------|----------|----------|
| 中文 | zh | zh-CHS | zh-CN |
| 英文 | en | en | en |
| 日文 | jp | ja | ja |
| 韩文 | kor | ko | ko |
| 法文 | fra | fr | fr |
| 德文 | de | de | de |
| 西班牙文 | spa | es | es |
| 俄文 | ru | ru | ru |
| 自动检测 | auto | auto | 不指定 |

---

## 六、使用限制与配额

### 6.1 百度翻译

| 版本 | 免费额度 | QPS限制 |
|------|----------|---------|
| 标准版 | 5万字符/月 | 10次/秒 |
| 高级版 | 100万字符/月 | 100次/秒 |

### 6.2 有道翻译

| 版本 | 免费额度 | QPS限制 |
|------|----------|---------|
| 体验版 | 1000次/小时 | 10次/秒 |
| 标准版 | 按量付费 | 100次/秒 |

### 6.3 谷歌翻译

| 版本 | 免费额度 | 价格 |
|------|----------|------|
| 基础版 | 50万字符/月 | $20/百万字符 |

---

## 七、常见问题

### Q1: 签名错误怎么办？

**A:** 请检查：
1. API密钥是否正确
2. 签名算法是否正确
3. 参数顺序是否符合要求
4. 时间戳是否有效

### Q2: 请求超时怎么办？

**A:** 请检查：
1. 网络连接是否正常
2. 是否需要代理访问
3. API服务是否正常

### Q3: 配额用完怎么办？

**A:** 
1. 升级API套餐
2. 切换到其他翻译引擎
3. 等待配额重置

---

## 八、安全建议

1. **密钥保护**：不要将API密钥提交到公开仓库
2. **加密存储**：本程序自动加密存储API密钥
3. **定期更换**：建议定期更换API密钥
4. **监控用量**：定期检查API使用量，防止异常调用

---

**文档版本**：v1.0  
**更新日期**：2024年
