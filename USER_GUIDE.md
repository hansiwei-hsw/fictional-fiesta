# Python 科学计算器 v2.0 使用说明

## 一、软件概述

Python 科学计算器 v2.0 是一款面向科研和工程人员的工程级计算器，采用模块化架构设计，支持基础四则运算和多种科学运算，具备历史记录、配置管理、缓存优化等高级功能。

### 主要特性

- **基础运算**：加、减、乘、除、取模
- **科学运算**：三角函数、对数、幂运算、阶乘、开方等（10+类）
- **交互式菜单**：清晰的命令行界面
- **历史记录**：自动保存运算记录，支持导出
- **配置管理**：可自定义角度模式、小数位数等
- **缓存优化**：高频计算结果缓存，提升性能
- **兼容模式**：支持旧版本输入格式

---

## 二、系统要求

- **操作系统**：Windows / macOS / Linux
- **Python版本**：Python 3.6 或更高版本
- **依赖**：仅使用 Python 标准库，无需安装第三方依赖

---

## 三、安装与启动

### 安装步骤

1. 下载项目文件到本地目录
2. 确保已安装 Python 3.6+
3. 无需安装额外依赖

### 启动方式

```bash
# 方式一：直接运行
python main.py

# 方式二：作为模块运行
python -m main
```

---

## 四、功能列表

### 4.1 基础运算

| 序号 | 功能 | 运算符 | 示例 |
|------|------|--------|------|
| 1 | 加法 | + | 5 + 3 = 8 |
| 2 | 减法 | - | 10 - 4 = 6 |
| 3 | 乘法 | × | 6 × 7 = 42 |
| 4 | 除法 | ÷ | 20 ÷ 4 = 5 |
| 5 | 取模 | % | 17 % 5 = 2 |

### 4.2 三角函数

| 序号 | 功能 | 说明 | 输入范围 |
|------|------|------|----------|
| 6 | 正弦 (sin) | 计算正弦值 | 任意实数 |
| 7 | 余弦 (cos) | 计算余弦值 | 任意实数 |
| 8 | 正切 (tan) | 计算正切值 | 非 90°+k·180° |
| 9 | 反正弦 (asin) | 计算反正弦值 | [-1, 1] |
| 10 | 反余弦 (acos) | 计算反余弦值 | [-1, 1] |
| 11 | 反正切 (atan) | 计算反正切值 | 任意实数 |

### 4.3 对数运算

| 序号 | 功能 | 说明 | 输入要求 |
|------|------|------|----------|
| 12 | 自然对数 (ln) | 以 e 为底的对数 | x > 0 |
| 13 | 常用对数 (log10) | 以 10 为底的对数 | x > 0 |
| 14 | 任意底对数 (log) | 指定底数的对数 | x > 0, base > 0 且 ≠ 1 |

### 4.4 幂运算与开方

| 序号 | 功能 | 说明 | 输入要求 |
|------|------|------|----------|
| 15 | 幂运算 (x^y) | x 的 y 次方 | 负数底需整数指数 |
| 16 | 平方根 (√) | 计算平方根 | x ≥ 0 |
| 17 | n次方根 | 计算 n 次方根 | n > 0，负数不能开偶次方 |

### 4.5 其他运算

| 序号 | 功能 | 说明 | 输入要求 |
|------|------|------|----------|
| 18 | 阶乘 (n!) | 计算阶乘 | 0 ≤ n ≤ 170 |
| 19 | 排列数 P(n,r) | 计算排列数 | n ≥ r ≥ 0 |
| 20 | 组合数 C(n,r) | 计算组合数 | n ≥ r ≥ 0 |
| 21 | 绝对值 (abs) | 计算绝对值 | 任意实数 |
| 22 | 自然指数 (e^x) | 计算 e 的 x 次方 | 任意实数 |
| 23 | 向下取整 (floor) | 向下取整 | 任意实数 |
| 24 | 向上取整 (ceil) | 向上取整 | 任意实数 |

### 4.6 系统功能

| 序号 | 功能 | 说明 |
|------|------|------|
| 25 | 查看历史记录 | 显示最近的运算记录 |
| 26 | 导出历史记录 | 导出为 txt/csv/json 格式 |
| 27 | 清空历史记录 | 清除所有历史记录 |
| 28 | 系统设置 | 配置计算器参数 |
| 29 | 查看缓存统计 | 显示缓存命中情况 |
| 30 | 帮助信息 | 显示使用帮助 |
| 0 | 退出程序 | 保存配置并退出 |

---

## 五、操作步骤

### 5.1 基本操作流程

1. **启动程序**：运行 `python main.py`
2. **查看菜单**：主菜单显示所有可用功能
3. **选择功能**：输入对应数字选择功能
4. **输入参数**：根据提示输入运算参数
5. **查看结果**：程序显示格式化的运算结果
6. **继续或退出**：选择其他功能或输入 `0` 退出

### 5.2 示例操作

#### 示例1：计算 sin(30°)

```
请选择功能: 6
请输入角度值（度）: 30

----------------------------------------
运算: sin(30度)
结果: 0.5
----------------------------------------
```

#### 示例2：计算阶乘

```
请选择功能: 18
请输入非负整数（最大170）: 10

----------------------------------------
运算: 10!
结果: 3628800
----------------------------------------
```

#### 示例3：计算组合数

```
请选择功能: 20
请输入总数n: 10
请输入选取数r（0-10）: 3

----------------------------------------
运算: C(10, 3)
结果: 120
----------------------------------------
```

---

## 六、系统设置

### 6.1 可配置项

| 设置项 | 说明 | 默认值 | 可选值 |
|--------|------|--------|--------|
| 角度模式 | 三角函数输入模式 | 角度 | 角度/弧度 |
| 小数位数 | 结果保留小数位 | 4 | 0-15 |
| 科学计数法 | 超大数自动转换 | 开启 | 开启/关闭 |
| 缓存机制 | 计算结果缓存 | 开启 | 开启/关闭 |
| 兼容模式 | 旧版本输入格式 | 关闭 | 开启/关闭 |
| 自动保存历史 | 自动保存运算记录 | 开启 | 开启/关闭 |
| 显示提示 | 启动时显示提示 | 开启 | 开启/关闭 |

### 6.2 修改设置

1. 在主菜单选择 `28` 进入系统设置
2. 输入对应数字修改设置项
3. 修改后自动保存

---

## 七、兼容模式

### 7.1 启用兼容模式

在系统设置中选择 `5` 开启兼容模式。

### 7.2 支持的输入格式

**二元运算格式**：`数字1 运算符 数字2`

```
5 + 3      → 8
10 - 4     → 6
6 * 7      → 42
20 / 4     → 5
2 ^ 10     → 1024
```

**一元运算格式**：`运算符 数字` 或 `数字 运算符`

```
sin 30     → 0.5（角度模式）
cos 60     → 0.5
sqrt 16    → 4
ln 2.718   → 1
fact 5     → 120
```

### 7.3 支持的运算符

| 运算符 | 别名 | 功能 |
|--------|------|------|
| + | | 加法 |
| - | | 减法 |
| * | x, × | 乘法 |
| / | ÷ | 除法 |
| % | | 取模 |
| ^ | | 幂运算 |
| sin | | 正弦 |
| cos | | 余弦 |
| tan | | 正切 |
| sqrt | | 平方根 |
| ln | | 自然对数 |
| log10 | | 常用对数 |
| fact | factorial | 阶乘 |
| abs | | 绝对值 |
| exp | | 自然指数 |

---

## 八、历史记录

### 8.1 查看历史记录

选择菜单 `25` 查看最近 20 条运算记录。

### 8.2 导出历史记录

选择菜单 `26` 可导出为以下格式：
- **TXT格式**：适合阅读和打印
- **CSV格式**：适合导入Excel等表格软件
- **JSON格式**：适合程序处理

### 8.3 清空历史记录

选择菜单 `27` 清空所有历史记录（需确认）。

---

## 九、快捷命令

在主菜单输入以下命令可快速执行操作：

| 命令 | 功能 |
|------|------|
| `help` | 显示帮助信息 |
| `config` | 打开系统设置 |
| `quit` / `exit` / `q` | 退出程序 |

---

## 十、文件说明

| 文件名 | 说明 |
|--------|------|
| `main.py` | 程序入口，交互控制 |
| `calculator_core.py` | 核心运算引擎 |
| `input_validator.py` | 输入校验模块 |
| `result_formatter.py` | 结果格式化模块 |
| `history_manager.py` | 历史记录管理 |
| `config_handler.py` | 配置管理模块 |
| `calculator_config.json` | 配置文件（自动生成） |
| `calculator_history.json` | 历史记录文件（自动生成） |

---

## 十一、常见问题

### Q1: 三角函数结果不正确？

**A:** 检查角度模式设置。默认为角度模式，如需使用弧度，请在系统设置中切换。

### Q2: 阶乘运算报错？

**A:** 阶乘仅支持非负整数（0-170），超出范围会报错。

### Q3: 对数运算报错？

**A:** 对数输入必须大于0，底数不能为1。

### Q4: 如何清空缓存？

**A:** 在菜单选择 `29` 查看缓存统计，然后输入 `y` 清空缓存。

### Q5: 历史记录丢失？

**A:** 确保 `自动保存历史` 设置为开启状态。历史记录保存在 `calculator_history.json` 文件中。

---

## 十二、技术支持

如遇问题，请检查：
1. Python 版本是否符合要求
2. 所有模块文件是否完整
3. 配置文件格式是否正确

---

**版本**：v2.0  
**更新日期**：2024年
